# REFLECTION: AI 辅助初稿

> 重要说明：本文件是 AI 辅助生成的反思报告初稿，用于帮助整理结构和证据。根据 AI4SE 作业要求，最终提交前必须由本人逐段改写、补充真实经历、删除不符合事实的内容，并在适当位置标注 AI 辅助范围。

## 反思正文草稿

这次 AI4SE 期末项目中，我选择将 CampusHub 校园互助服务平台整理为 B 类非 harness 应用项目提交。这个项目解决的是校园互助信息碎片化的问题：很多快递代取、学习求助、资料共享、二手交易和组队需求散落在聊天群中，信息难以检索，也缺少统一的订单状态和信用记录。CampusHub 试图把这些需求转化为结构化流程，让用户能发布需求、筛选需求、接单、完成订单、互相评价，并通过通知和管理后台维持基本秩序。

在使用 AI 辅助的过程中，我感受最明显的是：SPEC 和 PLAN 的质量直接决定了后续实现是否稳定。早期如果只说“帮我完善项目”，智能体往往会倾向于继续添加功能，例如推荐、举报、更多页面等。但真正对照 AI4SE 期末要求后，我发现项目最大的短板不是功能数量，而是工程提交闭环：缺少 SPEC、PLAN、SPEC_PROCESS、AGENT_LOG、REFLECTION，GitLab CI 也只是 smoke test，README 中还保留了团队课程项目的上下文。这说明智能体可以很快提出实现建议，但工程师必须先判断评分标准和项目目标，否则就会把努力用在低价值方向上。

Superpowers 方法论对我最有帮助的是它强制把“想做什么”和“如何验证做对了”分开。以前做课程项目时，容易把需求文档、实现计划和测试验证混在一起；这次整理时，我把问题陈述、用户故事、功能规约、非功能需求、安全边界、数据模型和验收标准写入 SPEC，再把具体整理任务写入 PLAN。这样做之后，项目不只是“能跑”，而是可以被另一个人或另一个智能体理解和复现。尤其是冷启动验证要求提醒我：如果一个全新的 agent 只看 SPEC 和 PLAN 就不知道怎么运行，那说明文档还没有真正承担工程接口的作用。

TDD 对 AI 协作既是约束，也是放大器。它的约束在于，不能让智能体直接堆实现，再事后补几个看似覆盖的测试；它的放大作用在于，一旦测试目标清晰，智能体可以围绕失败测试快速定位并修正行为。在这个项目中，历史功能并不是全部严格按“红-绿-重构”产生的，这是我需要如实承认的偏离。但现有后端已经有较完整的服务层和集成测试，`mvn test` 可以验证注册登录、需求、订单、评价、通知、管理后台等核心流程。后续如果继续开发，我会把每个需求拆成先写失败测试、再实现、再重构的 commit，而不是把 TDD 当作最后补交的形式。

Subagent-driven 工作流给我的启发是，任务颗粒度比模型能力本身更重要。如果一个 task 过大，例如“完善整个项目”，智能体会在文档、代码、测试、部署之间来回跳跃，很容易遗漏关键要求。更合适的颗粒度是 2 到 5 分钟能完成的小任务，例如“补 SPEC 的安全与凭据模型”“把 GitLab CI 改成包含 unit-test job”“核对 README 的端口说明”。这样的任务边界清晰、验证方式明确，也更适合并行 worktree 和后续 code review。

这次最典型的规约不清案例是端口配置。项目中曾同时出现后端默认 8080、前端代理 8092、MySQL 3307/3310、Redis 6379/6381 的不一致。对开发者本人来说，这些差异可能可以靠记忆绕过去，但对助教或冷启动 agent 来说就是运行失败的来源。通过整理 README 和 SPEC，我把本地开发策略固定为前端 5173、后端 8092、MySQL 3307、Redis 6379，并把生产环境交给 Docker Compose 内网服务名。这件事让我意识到，工程文档不是附属品，而是系统可运行性的一部分。

Prompt 和 context engineering 中最有效的策略，是先让智能体读取真实文件和作业要求，再让它给出判断，而不是直接让它生成答案。比如在判断是否需要新增功能时，我让 Codex 对照作业要求检查 README、CI、测试、Docker 和已有模块，最后得出的结论是“功能底子够，交付规范不足”。这个结论比单纯让 AI 发散功能更有价值。上下文越贴近真实仓库，智能体越像工程助手；上下文越抽象，它越像会写漂亮空话的文本生成器。

凭据与分发要求迫使我补上了以前容易忽略的问题。CampusHub 不调用外部 LLM 或付费 API，所以没有 OpenAI API Key 这类凭据；但它仍然有 JWT secret、数据库密码、Redis 连接信息等敏感配置。过去我可能会觉得课程项目用默认密码无所谓，但作业要求让我必须写清 `.env.example`、真实 `.env` 不提交、生产环境必须覆盖默认值、日志不能打印敏感信息。分发方面，Docker Compose 也让“我电脑上能跑”变成“别人能按 README 跑起来”的工程标准。

我对 Superpowers 的批判是，它假设项目可以从规约阶段开始，并且每一步都能留下干净的 TDD 和 PR 证据。但现实中，很多课程项目已经有历史代码、团队材料和不完整流程，后补规范时很难完全复原真实的红绿重构过程。因此我认为更合理的做法是诚实记录偏离，并从当前节点开始恢复纪律，而不是伪造一段完美流程。Superpowers 的价值不在于让项目看起来流程完美，而在于让工程师不断问：目标清楚吗？验证客观吗？凭据安全吗？别人能运行吗？AI 的输出有没有经过人的判断？

如果重做一次，我会从第一天就建立 SPEC、PLAN、AGENT_LOG 和 CI，而不是等项目成型后再整理。我也会把每个功能拆成更小的 PR，并在 commit message 中记录由哪个 agent 完成、人工修改了什么。这样最终提交时，项目的工程证据会自然生长出来，而不是后期补证。总体而言，这次项目让我更清楚地看到，当 LLM 能写大量代码时，人的价值并没有降低，而是转向了判断问题、设定边界、设计验证和承担责任。

## 提交前本人检查清单

- [ ] 删除或改写所有不符合本人真实经历的句子。
- [ ] 补充至少一个真实 prompt/context 策略案例。
- [ ] 补充最终 CI/CD pass 记录。
- [ ] 确认全文字数在 1500-2500 中文字左右。
- [ ] 在最终提交说明中标注 AI 辅助润色范围。
