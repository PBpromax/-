# CampusHub AI4SE 项目反思

我这次提交的项目是 CampusHub，一个面向校园互助场景的 Web 应用。它把散落在聊天群里的代取快递、学习求助、资料共享、二手交易和组队信息，整理成可以发布、接单、完成和评价的流程。项目本身并不是从 AI4SE 课程开始做的，真正的工作是把已有的课程项目重新放进 AI4SE 的工程要求中审视：能演示功能，不等于能交给陌生人复现、评审和运行。

最开始我把注意力放错了，以为需要增加推荐、举报或更多页面。对照作业要求后，问题反而很清楚：已有的发布需求、订单、评价、通知和后台模块已经足够构成一个应用；真正缺少的是规约、可执行的计划、过程记录、可信的 CI 和部署说明。把仓库交给不了解历史的人时，他并不能立刻判断怎样启动、哪些配置不能提交、测试是否真的跑过。

因此，SPEC 和 PLAN 在这次工作里比“继续写功能”更重要。SPEC 迫使我补上过去没有认真写过的内容：用户是谁，订单状态怎样变化，异常输入怎样处理，JWT 密钥和数据库密码可能在哪里泄露，Docker 运行时各服务如何连接。PLAN 则把这些问题拆到文件和验证命令的层面，让“做完”有了可检查的标准。

我也意识到，规约写得不清楚时，智能体很容易做出表面合理、实际不合适的补充。端口就是一个例子：项目历史里曾同时出现后端 8080、前端代理 8092，以及不同 MySQL 和 Redis 端口。后来我把本地开发明确为前端 5173、后端 8092、MySQL 3307、Redis 6379，并把生产环境的服务发现交给 Docker Compose 内部网络。没有写下来的约定，往往只存在于写代码的人脑子里。

课程要求中的 Superpowers 工作流提醒我，不能把“让 AI 写”和“对结果负责”混为一件事。brainstorming 会追问目标用户、边界和验收条件；writing-plans 则要求任务带上验证步骤。CampusHub 的核心实现早于这次课程整理，我无法诚实地把历史代码描述成严格按红、绿、重构逐个提交的成果，也没有为每段旧代码补造 PR 记录。我选择把这个偏离写进过程文档，并从现在的配置、文档和 CI 改动开始保留证据。

这也影响了我对 TDD 的看法。TDD 在 AI 协作中不是阻碍，前提是测试描述的是明确行为。没有测试时，智能体很容易给出“看起来完整”的实现，边界条件却没有被验证；有了失败测试，它就更容易在有限范围内定位问题。这个项目的后端测试覆盖登录、需求、订单、评价、通知和管理后台等主要路径，`mvn test` 通过了 85 个测试。它们不能证明所有历史功能都采用了 TDD，但提供了可重复执行的回归检查。以后新增功能时，我会先写失败测试，再保留失败和通过的记录。

subagent 工作流的实际经验也让我改变了对“一个任务”的理解。像“完善整个项目”这样的指令太宽，智能体会在 README、CI、Docker、业务代码之间来回切换，最后往往每项都做了一点却没有闭环。更合适的任务是“补充 SPEC 的凭据威胁模型”“让 CI 真正执行后端单元测试”“核对 README 中开发和生产端口是否一致”这类小任务。它们通常在几分钟内有明确结果，也可以独立审查。对我来说，subagent 能自主推进多久，并不取决于它能生成多少文件，而取决于输入是否包含足够的上下文、停止条件和验收方式。一旦它遇到产品取舍或真实环境信息，仍然需要人来决定。

我后来使用 AI 时最有效的做法，是先提供真实仓库和作业要求，再要求它指出缺口。抽象地问“怎样让项目更好”，得到的通常是泛化的功能建议；让它读取 CI、Docker Compose、README、测试目录和评分标准后，它才能指出具体问题，例如原来的 GitLab CI 只是 smoke test、文档仍是团队课程交付口径、生产配置里存在默认值风险。这个策略并不节省人的判断，相反，它把人的工作集中到确认优先级和拒绝不必要改动上。

凭据和分发是我此前最容易忽略的两部分。CampusHub 不调用付费 LLM 服务，因此没有需要录入的 LLM API key；但 JWT secret、数据库用户名和密码同样属于敏感配置。现在仓库提供 `.env.example`，生产配置要求从环境变量读取值，部署脚本会检查必填变量，README 也说明 `.env` 是明文文件，不能提交到 Git。Docker Compose 的意义也不只是“有一个 Dockerfile”：它要求我回答一个更难的问题，别人拿到仓库后怎样在一台新机器上启动依赖服务，并且不把自己的密码写进代码。

CI 的迁移是这次整理里一个实际发生的转折。学校 GitLab 的 pipeline 因平台问题无法稳定通过；确认这一点后，我把仓库迁移到公开 GitHub，并配置 GitHub Actions 运行后端单元测试、前端构建和 Docker 构建，最后一次记录为通过。线上部署 URL 仍需要在最终提交前补齐，这是目前最明确的未完成项。

如果重新开始，我会在第一周就建立 SPEC、PLAN、AGENT_LOG 和 CI，把每个功能拆成小分支和可复验的提交，而不是在项目接近完成时再补齐工程证据。我对 Superpowers 的保留意见是，它默认项目能够从规约开始，而且每一步都能留下干净的 TDD、worktree 和 PR 轨迹；现实中的课程项目常常有遗留代码、多人协作痕迹和环境故障。它不应被当作一张用来证明流程完美的清单。它真正有价值的地方，是反复逼着开发者回答几个不太舒服的问题：需求是否清楚，测试是否真的验证了行为，配置是否安全，别人能否从零运行，以及 AI 输出是否经过了人的审查和承担。

本文基于项目仓库、测试和 CI 记录整理，并使用 AI 辅助检查结构和润色表达。提交前应由本人逐项核对是否符合实际参与过程，并按真实经历补充或删改；不得将未经本人确认的 AI 表述作为个人经历提交。
