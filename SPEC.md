# SPEC: CampusHub AI4SE Final Project

## 1. 问题陈述

大学校园里的互助需求常散落在微信群、QQ群、朋友圈和论坛中，信息生命周期短、检索困难、身份信任弱，导致“有人需要帮助”和“有人愿意提供帮助”之间匹配效率低。CampusHub 通过一个统一的校园互助平台，把需求发布、接单、订单状态、评价信用和消息通知整合到同一条流程中。

目标用户包括：

- 需求方：需要快递代取、学习辅导、资料共享、二手交易、活动组队等校园互助服务的学生。
- 服务方：愿意接取轻量任务、赚取报酬或积累校园互助信用的学生。
- 管理员：负责处理违规需求、查看平台运行数据和维护平台秩序的管理者。

项目属于 AI4SE B 类非 harness 应用项目。系统不实现 autonomous agent 主循环，不调用外部付费 LLM API。

## 2. 用户故事

1. 作为学生用户，我希望能注册并登录账号，以便安全地发布或接取校园互助需求。
2. 作为需求方，我希望能填写标题、描述、预算和分类发布需求，以便把互助请求结构化地展示给其他同学。
3. 作为服务方，我希望能按分类和关键字浏览需求，以便快速找到适合自己的任务。
4. 作为订单参与者，我希望订单状态从接单到验收再到完成有明确流转，以便双方知道当前责任归属。
5. 作为平台用户，我希望能在完成订单后互相评价，以便信用分反映协作质量。
6. 作为用户，我希望能收到注册、订单、评价等通知，以便及时了解与自己相关的平台事件。
7. 作为管理员，我希望能查看用户、需求和订单，并下架违规需求，以便维护平台秩序。

## 3. 功能规约

### 3.1 用户与认证

- 输入：用户名、密码、校区、学号等注册信息；用户名和密码登录信息。
- 行为：注册时检查用户名唯一性并使用 BCrypt 存储密码哈希；登录成功后签发 JWT；受保护接口从 `Authorization: Bearer <token>` 解析用户身份。
- 输出：统一 JSON 响应，登录成功返回 `userId`、`role` 和 `token`。
- 边界与错误：用户名重复返回业务错误；密码错误和用户不存在返回明确错误；缺失或非法 JWT 返回未认证。

### 3.2 个人资料

- 输入：昵称、校区、学院、专业、年级、简介、联系方式可见性等资料。
- 行为：用户只能查看和修改自己的资料。
- 输出：个人资料响应对象。
- 边界与错误：未登录不可访问；不存在用户返回业务错误。

### 3.3 需求大厅

- 输入：需求标题、描述、预算、分类；列表页分页、分类、关键字筛选参数。
- 行为：登录用户发布需求；需求默认进入待接单状态；大厅支持分页、分类和关键字检索；详情页展示发布者和状态。
- 输出：需求列表、需求详情、创建结果。
- 边界与错误：预算必须为正；标题和描述不能为空；发布者不能接自己的需求；已接单或已完成需求不可重复接单。

### 3.4 订单流程

- 输入：需求 ID、订单 ID、状态变更请求。
- 行为：服务方接单生成订单并把需求状态改为已接单；服务方提交验收；需求方确认完成；系统维护订单状态和完成时间。
- 输出：订单详情、我发布的订单列表、我接取的订单列表。
- 边界与错误：非订单参与者不可查看详情；非法状态跳转返回业务错误；同一需求只能被接单一次。

### 3.5 评价信用

- 输入：订单 ID、星级、评价内容。
- 行为：订单完成后双方各可评价一次；评价写入评价表并调整被评价方信用分。
- 输出：评价提交结果、更新后的个人信用信息。
- 边界与错误：未完成订单不可评价；重复评价不可提交；星级限制为 1 到 5。

### 3.6 消息通知

- 输入：通知分页参数、标记已读请求。
- 行为：系统在注册、订单、评价等事件中创建通知；用户可查看自己的通知、未读数并标记已读。
- 输出：通知列表、未读数量、更新结果。
- 边界与错误：用户不能读取或修改他人通知。

### 3.7 管理后台

- 输入：管理员查询参数、需求下架请求。
- 行为：管理员查看用户、需求、订单数据；对违规或过期需求执行下架。
- 输出：后台列表与操作结果。
- 边界与错误：普通用户访问管理接口返回无权限；下架不存在需求返回业务错误。

## 4. 非功能性需求

- 可运行性：全新机器在安装 JDK、Node、Docker 后，应能按照 README 在本地启动。
- 可测试性：后端核心服务具备 H2 测试，前端至少可执行生产构建。
- 性能：普通列表查询支持分页；单机演示环境满足课程验收和小规模并发访问。
- 可用性：核心流程应能通过两个普通账号完成“发布-接单-验收-完成-评价”闭环。
- 可观测性：后端使用统一异常处理和日志；CI 上传 Surefire 报告。
- 兼容性：开发环境面向 macOS/Linux；容器部署依赖 Docker Compose v2。

## 5. 安全与凭据威胁模型

本项目不调用外部付费 LLM API，因此没有 OpenAI、Claude 等 API Key 存储需求。需要保护的敏感信息是：

- `CAMPUSHUB_JWT_SECRET`
- `DB_USERNAME`
- `DB_PASSWORD`
- `DB_ROOT_PASSWORD`
- Redis 连接信息

威胁与对策：

- 凭据进入 Git：提供 `.env.example`，真实 `.env` 不提交；README 明确说明。
- 默认弱口令用于生产：生产 Compose 对 `DB_USERNAME`、`DB_PASSWORD`、`DB_ROOT_PASSWORD`、`CAMPUSHUB_JWT_SECRET` 使用必填变量校验；缺失时启动失败。
- 日志泄露 token 或密码：业务日志不打印密码、JWT 明文和数据库密码。
- 未授权接口访问：后端拦截 `/api/**`，排除认证和健康检查接口；受保护接口依赖 JWT。
- 越权访问：订单、通知、资料、管理后台需要服务层校验用户身份和角色。

## 6. 系统架构

组件：

- Web 前端：Vue 3 单页应用，负责页面渲染、路由守卫、表单交互、JWT 本地保存和 API 调用。
- API 后端：Spring Boot REST 服务，封装认证、需求、订单、评价、通知、管理后台等业务模块。
- MySQL：持久化用户、需求、订单、评价、通知等关系数据。
- Redis：为后续会话、缓存、热点需求和并发控制预留；当前作为基础设施纳入运行环境。
- Nginx：生产容器中托管前端静态资源，并把 `/api/*` 代理到后端容器。

数据流：

1. 用户在前端提交注册/登录请求。
2. 后端验证输入，读写 MySQL，登录成功签发 JWT。
3. 前端后续请求携带 JWT；后端拦截器解析用户 ID。
4. 用户发布需求后，其他用户浏览并接单，订单状态随操作更新。
5. 订单事件触发通知；完成后双方评价并更新信用分。

## 7. 数据模型

主要实体：

- `sys_user`：用户 ID、用户名、密码哈希、学号、校区、学院、专业、年级、简介、联系方式可见性、信用分、角色、时间戳。
- `biz_requirement`：需求 ID、发布者 ID、标题、描述、预算、分类、状态、创建和更新时间。
- `biz_order`：订单 ID、需求 ID、接单者 ID、金额、状态、创建时间、完成时间。
- `biz_evaluation`：评价 ID、订单 ID、评价方 ID、被评价方 ID、星级、内容、创建时间。
- `biz_notification`：通知 ID、用户 ID、标题、内容、类型、已读状态、创建时间。

关系与约束：

- 一个用户可发布多个需求。
- 一个需求最多对应一个有效订单。
- 一个订单关联一个需求和一个接单者。
- 一个完成订单最多允许双方各评价一次。
- 通知按用户隔离读取和更新。

## 8. 凭据与分发设计

分发形态选择 Docker Compose。目标机器需要安装 Docker 和 Docker Compose v2。

获取与运行：

1. 克隆仓库。
2. 复制 `.env.example` 为 `.env`。
3. 将默认密码和 JWT secret 替换为部署环境专用值。
4. 执行生产 Compose 构建与启动。

配置方式：

- 本地开发可使用 README 中命令行环境变量覆盖。
- 生产演示使用 `.env` 或 CI/CD secret 注入。
- `.env` 是明文文件，应限制文件权限，不进入 Git，不通过截图或日志公开。
- `.env.example`、`docker-compose-prod.yml` 和 Spring prod profile 共享同一组变量名：`DB_USERNAME`、`DB_PASSWORD`、`DB_ROOT_PASSWORD`、`CAMPUSHUB_JWT_SECRET`。

## 9. 技术选型理由

- Vue 3 + Vite：适合快速构建课程项目 Web UI，开发反馈快。
- Element Plus：提供成熟表单、表格、按钮、弹窗等组件，降低 UI 实现风险。
- Spring Boot：适合构建结构清晰的 REST 后端，便于分层测试。
- MyBatis-Plus：降低常规 CRUD 与分页查询样板代码。
- MySQL：适合订单、评价、用户等关系模型。
- Redis：为缓存和后续并发控制预留基础设施。
- Docker Compose：满足 AI4SE 分发要求，便于助教在新机器上启动完整系统。

前端 UI 未正式引入 Open Design skill；本次提交在 SPEC_PROCESS 中记录为约束偏离，原因是项目已有 Vue/Element Plus UI 基础，期末整理优先保证可运行、测试和分发闭环。

## 10. 验收标准

- 注册登录：新用户能注册，能用正确密码登录，错误密码返回业务错误。
- 需求流程：登录用户能发布需求，列表可筛选，详情可查看。
- 订单流程：非发布者能接单，发布者不能自接单，状态能从进行中到待确认再到完成。
- 评价流程：完成订单双方可评价一次，重复评价被拒绝，信用分更新。
- 通知流程：用户可查看未读数量，标记已读后数量更新。
- 管理后台：管理员可访问后台并下架需求，普通用户无权限。
- 测试：`mvn test` 通过，前端 `npm run build` 通过。
- 分发：`docker compose -f docker-compose-prod.yml build` 成功，生产 Compose 可启动 Web UI。

## 11. 风险与未决问题

- 历史配置中存在开发端口不一致，README 已统一以 `5173` + `8092` 说明。
- 初始化数据中的旧账号密码可能不可登录，演示建议通过注册页创建账号。
- 已部署公网入口：`http://101.132.133.42`。当前部署以宿主机 Nginx 将公网 `80` 端口反向代理至仅监听 `127.0.0.1:8080` 的 CampusHub Nginx 容器，以避免影响服务器预装的 OMS 服务。
- `REFLECTION.md` 已整理为提交候选稿，最终文本仍必须由本人确认真实经历并标注 AI 辅助范围。
- 没有接入真实短信、邮箱、支付和学校统一认证，系统仅作为课程级可演示应用。
