spring:
  datasource:
    url: jdbc:mysql://localhost:3310/campushub_db?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ch_dev
    password: ch_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6381
      database: 0
      timeout: 3000ms

logging:
  level:
    com.campushub: debug
